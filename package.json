{
  "name": "kb-template",
  "version": "0.1.0",
  "description": "Knowledge-blocks template — fork into kb-{topic} to start a new reference site",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "d3-force": "^3.0.0"
  }
}
